part of masamune.flutter;

/// Define the default path.
class DefaultPath {
  /// Routes root.
  static const String route = "route";

  /// Data loaded on the page.
  static const String pageData = "page/data";

  /// Dialog title.
  static const String dialogTitle = "dialog/title";

  /// Dialog text.
  static const String dialogText = "dialog/text";

  /// Dialog submit button text.
  static const String dialogSubmitText = "dialog/submit/text";

  /// Action when the submit button of the dialog is pressed.
  static const String dialogSubmitAction = "dialog/submit/action";

  /// Dialog cancel button text.
  static const String dialogCancelText = "dialog/cacnel/text";

  /// Action when the cancel button of the dialog is pressed.
  static const String dialogCancelAction = "dialog/cancel/action";

  /// Loop index.
  static const String loopIndex = "loop/index";

  /// Document path in loop.
  static const String loopItem = "loop/item";

  /// Tap event during loop.
  static const String loopTap = "loop/tap";

  /// Long tap event during loop.
  static const String loopTapLong = "loop/tap/long";

  /// Double tap event during loop.
  static const String loopTapDouble = "loop/tap/double";

  /// Form data on the page.
  static const String formData = "page/form";
}
